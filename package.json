{
  "name": "gazy",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension: extract, score, and export LinkedIn people-search results. Written in TypeScript; `npm run build` compiles src/ to dist/, which is the folder loaded as the unpacked extension.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "check": "npm run typecheck && npm run lint && npm run build && node --test",
    "test": "npm run check",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/chrome": "^0.2.6",
    "eslint": "^9.39.5",
    "globals": "^17.11.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.67.0"
  }
}
